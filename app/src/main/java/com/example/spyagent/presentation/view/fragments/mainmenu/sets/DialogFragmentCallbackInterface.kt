package com.example.spyagent.presentation.view.fragments.mainmenu.sets

interface DialogFragmentCallbackInterface {

    fun updateWord(oldWord: String, newWord:String)
    fun createNewWord(newWord:String)
}